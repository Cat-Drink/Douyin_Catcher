# v0.3.0 后端代码来源审查记录

## 审查范围

本次静态审查覆盖以下与上游项目功能最接近的文件：

- `crawlers/signer/xbogus.py`
- `crawlers/signer/abogus.py`
- `crawlers/video_parser.py`
- `crawlers/user_home_crawler.py`
- `crawlers/api_spec.py`

## 可验证证据

- 仓库代码中未发现 `Evil0ctal` 包导入、上游 Git URL 依赖或 vendored 上游目录。
- `requirements.txt` 未声明上游项目依赖。
- 签名模块的模块说明明确写出“纯 Python 自主实现”，并将上游项目描述为设计思路参考，而非代码来源。
- README 和项目架构文档将上游项目列为公开设计/原理参考。
- 上述文件确实使用相同或相近的抖音公开 API 路径、字段模型、签名体系和媒体 URL 处理规则；这些是需要持续人工比对的功能相似性风险，不足以单独证明复制了著作权代码。

## 残余风险与后续规则

静态检索不能证明不存在逐段复制、相同测试向量或历史来源问题，也不能替代对具体版本的逐函数比对。因此发布前如出现代码来源争议，应对相关上游提交和本项目历史逐段比对；若确认包含第三方代码或数据，应保留其版权/许可证声明，或在发布前完成独立重写。

本记录只描述仓库中可复查的工程证据，不构成法律意见，也不代表上游作者对本项目授权或背书。
